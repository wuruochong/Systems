#include <stdio.h>
#include <sstdlib.h>

int main(){
  int sum;
  int three = 3;
  int five = 5;
  int count3 = 0;
  int count5 = 0;

  while (three < 1000){
    sum+= three;
    count3 +=1;
    three *= count3;
  }

  while (five < 1000){
    sum+= five;
    count +=1;
    five *= count5;
  }
  printf("result: %d",sum);
  return sum;
}
