#include <stdio.h>
#include <stdlib.h>

int main(){
  int sum = 0;
  int count = 3;
  for (count ; count <1000 ; count++){
    if ((count%3 == 0) || (count%5 == 0)){
      sum += count;
    }
  }
  printf("Question 1 answer: %d \n",sum);
  return sum;
}
